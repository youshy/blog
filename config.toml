baseurl = "https://akondas.com/"
languageCode = "en-gb"
theme = "terminal"
paginate = 5

googleAnalytics = "UA-166121756-1"

summaryLength = 20

[permalinks]
  posts = "/blog/:title"

[params]
  # dir name of your main content (default is `content/posts`).
  # the list of set content will show up on your index page (baseurl).
  contentTypeName = "posts"

  # ["orange", "blue", "red", "green", "pink"]
  themeColor = "orange"

  # if you set this to 0, only submenu trigger will be visible
  showMenuItems = 6

  # show selector to switch language
  showLanguageSelector = false

  # set theme to full screen width
  fullWidthTheme = false

  # center theme with default width
  centerTheme = true

  # set a custom favicon (default is a `themeColor` square)
  favicon = "images/favicon.ico"

  # set post to show the last updated
  # If you use git, you can set `enableGitInfo` to `true` and then post will automatically get the last updated
  showLastUpdated = false
  # Provide a string as a prefix for the last update date. By default, it looks like this: 2020-xx-xx [Updated: 2020-xx-xx] :: Author
  # updatedDatePrefix = "Updated"

  # set all headings to their default size (depending on browser settings)
  # it's set to `true` by default
  oneHeadingSize = false

[params.linkedin]
  creator = "Artur Kondas"
  site = "https://www.linkedin.com/in/arturkondas/"

[languages]
  [languages.en]
    languageName = "English"
    title = "Artur Kondas"
    subtitle = "Golang, Programming, Open Source. Lessons. Also Pizza."
    owner = "Artur Kondas"
    keywords = "Go, Golang, Programming, Open Source, Lessons, Pizza"
    copyright = ""
    menuMore = "Show more"
    readMore = "Read more"
    readOtherPosts = "Read other posts"
    newerPosts = "Newer posts"
    olderPosts = "Older posts"
    missingContentMessage = "These are not the droids you're looking for..."
    missingBackButtonLabel = "Back to home page"

    [languages.en.params.logo]
      logoText = "Artur Kondas"
      logoHomeLink = "/"

    [languages.en.menu]
      [[languages.en.menu.main]]
        identifier = "About"
        name = "About"
        url = "/about"
        weight = 1
      [[languages.en.menu.main]]
        identifier = "Media"
        name = "Media"
        url = "/media"
        weight = 2
      [[languages.en.menu.main]]
        identifier = "Write For Me"
        name = "Write For Me"
        url = "/write-for-me"
        weight = 3
      [[languages.en.menu.main]]
        identifier = "Open Source"
        name = "Open Source"
        url = "/opensource"
        weight = 4
      [[languages.en.menu.main]]
        identifier = "All Posts"
        name = "All Posts"
        url = "/posts"
        weight = 5
      [[languages.en.menu.main]]
        identifier = "Tags"
        name = "Tags"
        url = "/tags"
        weight = 6
      [[languages.en.menu.main]]
        identifier = "Subscribe"
        name = "Subscribe"
        url = "/subscribe"
        weight = 7

